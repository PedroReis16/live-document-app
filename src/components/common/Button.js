// Botão customizado com estilos consistentes
const Button = ({ title, onPress, variant, disabled, loading }) => {
  // Variantes: primary, secondary, danger, outline
  // Estados: normal, disabled, loading
  // Animações de press
};
