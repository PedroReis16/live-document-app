// Input field com validação visual
const Input = ({
  placeholder,
  value,
  onChangeText,
  error,
  secureTextEntry,
  multiline,
}) => {
  // Estados de erro e foco
  // Ícones opcionais
  // Validação em tempo real
};
