// Formulário de login
const LoginForm = ({ onSubmit, loading }) => {
  // Campos: email/username, senha
  // Validação local
  // "Lembrar-me" checkbox
  // Link para registro
  // Recuperação de senha
};
