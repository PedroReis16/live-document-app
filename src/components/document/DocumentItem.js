// Item da lista de documentos
const DocumentItem = ({ document, onPress, onShare, onDelete }) => {
  // Preview do conteúdo
  // Indicadores: compartilhado, offline, sincronizando
  // Ações: editar, compartilhar, deletar
  // Data de modificação
  // Avatar dos colaboradores
};
